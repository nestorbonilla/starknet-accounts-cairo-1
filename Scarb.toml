[workspace]
members = ["src/hello"]
name = "starknet_accounts"
version = "0.1.0"

# See more keys and their definitions at https://docs.swmansion.com/scarb/docs/reference/manifest.html

[workspace.dependencies]
starknet = ">=2.0.0"

[scripts]
all = "scarb build && scarb test"
